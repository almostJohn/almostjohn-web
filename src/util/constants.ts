export const projectInfo = {
	tsConfigWeb: {
		title: "ts-config-web",
		description:
			"Generate clean, opinionated tsconfig.json setups for modern TypeScript projects.",
		url: {
			github: "https://github.com/almostJohn/tsconfig-web",
			live: "https://app-tsconfig.vercel.app",
		},
		tech: ["next.js", "react", "tailwindcss", "typescript"],
	},
};

export const NAME = "John";
export const ROLE = "Full Stack Web Developer";

export const NAME_MAX_LENGTH = 32;
export const NAME_MIN_LENGTH = 2;

export const SUBJECT_MAX_LENGTH = 32;
export const SUBJECT_MIN_LENGTH = 2;

export const MESSAGE_MAX_LENGTH = 350;
export const MESSAGE_MIN_LENGHT = 2;

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
